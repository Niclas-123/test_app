<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <%= link_to 'Alpha-Blog', root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li><%= link_to 'About', about_path, class: "nav-link" %> </li>
                <li><%= link_to 'Users', users_path, class: "nav-link" %> </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Articles
                    </a>
                    <ul class="dropdown-menu">
                        <li><%= link_to 'Articles listing', articles_path, class: "nav-link dropdown-item" %> </li>
                        <li><%= link_to 'New Article', new_article_path, class: "nav-link dropdown-item" %></li>
                    </ul>
                </li>
            </ul>
            <form class="d-flex">
                <% if logged_in? %>
                    <%= link_to current_user.username, user_path(current_user), class: "nav-link" %>
                    <%= link_to 'Log out', logout_path, class: "btn btn-outline-primary", method: :delete %>
                <% else %>
                    <%= link_to 'Sign up', signup_path, class: "btn btn-outline-primary me-2" %>
                    <%= link_to 'Log in', login_path, class: "btn btn-outline-primary" %>
                <% end %>  
            </form>
        </div>
    </div>
</nav>